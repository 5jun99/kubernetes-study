# ===================== C Service =====================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: c-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: c-service
  template:
    metadata:
      labels:
        app: c-service
    spec:
      containers:
        - name: c-service
          image: 5jun99/c-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
            successThreshold: 1
---
apiVersion: v1
kind: Service
metadata:
  name: c-service
spec:
  type: ClusterIP
  selector:
    app: c-service
  ports:
    - port: 80
      targetPort: 8080
